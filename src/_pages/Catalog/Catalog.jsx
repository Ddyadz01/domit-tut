import React from 'react';

import { CenterContent } from '../../_components/IndexComponents';

const Catalog = () => {
  return (
    <CenterContent>
      {/* {products.map((item) => (
        <div>{item.title}</div>
      ))} */}
    </CenterContent>
  );
};

export default Catalog;
